import { Button } from 'antd';
import React from 'react';

import './LargeButton.scss';

const LargeButton = () => {
  return (
    <Button className="button__wrapper" type="primary">
      Click me
    </Button>
  );
};

export default LargeButton;
